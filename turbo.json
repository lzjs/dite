{
  "$schema": "https://turborepo.org/schema.json",
  "baseBranch": "origin/master",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "build2": {
      "dependsOn": ["^build2"],
      "outputs": ["dist/**"]
    },
    "build:deps": {
      "dependsOn": ["^build:deps"],
      "cache": false,
      "outputs": ["compiled/**"]
    },
    "test": {
      "outputs": [".jest-cache/**"]
    }
  },
  "globalDependencies": [
    "tsconfig.base.json",
    "tsconfig.json",
    "jest.config.ts"
  ]
}
